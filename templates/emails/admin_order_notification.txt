NEW ORDER RECEIVED!

Order #{{ order.id }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

CUSTOMER INFORMATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Name: {{ customer_name }}
Email: {{ customer_email }}
Phone: {{ order.phone }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SHIPPING ADDRESS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

{{ order.address }}
{{ order.city }}, {{ order.postal_code }}
{% if order.country %}{{ order.country }}{% endif %}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ORDER ITEMS ({{ currency_code }})
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

{% for row in email_item_rows %}
{% with item=row.order_item %}
{{ item.quantity }}x {{ item.product.name }}{% if item.variant %} (Size: {{ item.variant.size }}){% endif %}
   {{ item.quantity }} × {{ currency_symbol }}{{ row.unit_display|floatformat:2 }} each

{% endwith %}
{% endfor %}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ORDER SUMMARY
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Subtotal:        {{ currency_symbol }}{{ subtotal|floatformat:2 }}
Shipping:        {{ currency_symbol }}{{ shipping_cost|floatformat:2 }}
{% if discount_amount > 0 %}Discount:        -{{ currency_symbol }}{{ discount_amount|floatformat:2 }}
{% endif %}
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TOTAL:           {{ currency_symbol }}{{ total|floatformat:2 }} {{ currency_code }}
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

{% if order.shipping_option %}
Shipping Method: {{ order.shipping_option.name }}
Delivery Time: {{ order.shipping_option.delivery_time }}
{% endif %}

{% if order.coupon %}
Coupon Used: {{ order.coupon.code }}
{% endif %}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

View order in admin panel:
{{ admin_url }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Order placed on {{ order.created_at|date:"F d, Y" }} at {{ order.created_at|time:"H:i" }}

This is an automated notification from Marbaras e-commerce system.
