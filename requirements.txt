asgiref==3.10.0
certifi==2025.10.5
charset-normalizer==3.4.4
Django==5.2.7
django-allauth==65.12.1
PyJWT==2.9.0
cryptography==43.0.3
idna>=2.5,<2.9
pillow==12.0.0
python-dotenv==1.1.1
requests==2.21.0
sqlparse==0.5.3
stripe==13.0.1
typing_extensions==4.15.0
urllib3==2.5.0

# Production server
gunicorn==23.0.0

# Database drivers
mysqlclient==2.2.4
psycopg2-binary==2.9.9  # PostgreSQL support (for Render, Railway, etc.)

# Caching (optional, for Redis)
django-redis==5.4.0
redis==5.0.1

# Rate limiting (optional)
django-ratelimit==4.1.0

# Email API services (for Railway Hobby plan - no SMTP support)
# Resend is Railway's recommended email service
# requests is already installed, but listed here for clarity
# requests==2.21.0  # Already installed above

# Cloud storage for media files (Cloudinary)
cloudinary==1.44.1
django-cloudinary-storage==0.3.0

# Shipping API (EasyPost)
easypost==7.0.0

# Shipping API (Shippo)
shippo==2.0.0
